export interface ICollaborator {
  id: string;
  name: string;
  position: string;
  image: string;
  isFavorite: boolean;
}

export interface ITeam {
  id: string;
  name: string;
  color: string;
}
